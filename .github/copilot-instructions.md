# Copilot Instructions

## Project Guidelines
- Preferences: Clean up all project source files when requested; preserve existing coding style (tabs/spaces/brace style) and avoid functional changes unless explicitly allowed; run build after edits to verify compilation.